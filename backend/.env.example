# Copy this file to backend/.env on the server.
# Never commit the real .env file.

NODE_ENV=production
HOST=127.0.0.1
PORT=3000

# Absolute paths are recommended in production.
DB_PATH=/srv/server-mod/backend/data/app.sqlite

# Enable only when running behind a trusted reverse proxy such as Nginx.
TRUST_PROXY=true
